# RAG项目意图识别方案建议

## 方案概述

针对你的RAG项目，这里提供几种意图识别的实施方案，从简单到复杂，你可以根据实际需求选择。

---

## 方案一：基于规则的方法（简单快速）

### 适用场景
- 需要快速实现基本意图分类
- 意图类型明确且数量较少
- 对性能要求高

### 实现方式

```python
# backend/app/utils/intent_classifier.py
class RuleBasedIntentClassifier:
    def __init__(self):
        # 定义关键词映射
        self.intent_keywords = {
            "chat": ["你好", "嗨", "在吗", "聊天", "聊聊", "问候"],
            "document_search": ["文件", "文档", "资料", "在哪", "查找", "搜索"],
            "weather": ["天气", "温度", "下雨", "晴天"],
            "calculation": ["计算", "算一下", "多少", "加", "减", "乘", "除"],
            "chatbot": ["你是谁", "介绍一下", "功能", "怎么用"]
        }
    
    def classify(self, query: str) -> str:
        query_lower = query.lower()
        
        # 计算每个意图的匹配分数
        scores = {}
        for intent, keywords in self.intent_keywords.items():
            score = sum(1 for kw in keywords if kw in query_lower)
            scores[intent] = score
        
        # 返回得分最高的意图
        if max(scores.values()) > 0:
            return max(scores, key=scores.get)
        return "rag"  # 默认走RAG流程
```

### 使用方式

```python
# 在chat_router.py中添加
from ..utils.intent_classifier import RuleBasedIntentClassifier

classifier = RuleBasedIntentClassifier()

@router.post("/chat/stream")
def chat_stream(req: ChatRequest):
    # 1. 意图识别
    intent = classifier.classify(req.question)
    
    if intent == "chat":
        # 纯聊天，不检索文档
        answer, thinking_text = chat_without_rag(req.question)
    elif intent == "weather":
        # 调用天气API
        answer = get_weather(req.question)
    else:
        # 走RAG流程
        generator = rag_service.ask_rag_stream(...)
    
    return StreamingResponse(...)
```

### 优点
- 实现简单，开发快速
- 性能高，不依赖外部服务
- 可解释性强

### 缺点
- 需要手动维护关键词
- 对复杂问题分类效果有限
- 难以扩展

---

## 方案二：基于LLM分类的方法（推荐）

### 适用场景
- 意图类型较多且复杂
- 需要理解语义而非关键词
- 希望有较好的扩展性

### 实现方式

```python
# backend/app/utils/intent_classifier.py
from ..services.generator_service import chat

class LLMIntentClassifier:
    def __init__(self, model="glm-4.5-flash"):
        self.model = model
        
        # 定义系统提示
        self.system_prompt = """你是一个意图分类器。请根据用户的问题判断其意图，只返回以下标签之一：
chat - 闲聊、问候、社交类问题
weather - 天气相关查询
document_search - 文件、文档搜索
calculation - 数学计算
recommendation - 推荐请求
rag - 需要基于知识库回答的问题

只返回标签，不要其他解释。"""
    
    def classify(self, query: str) -> str:
        # 构建分类提示
        messages = [
            {"role": "system", "content": self.system_prompt},
            {"role": "user", "content": f"用户问题：{query}"}
        ]
        
        # 调用LLM进行分类
        response, _ = chat(messages, model=self.model)
        
        # 提取意图标签
        intent = response.strip().lower()
        valid_intents = ["chat", "weather", "document_search", "calculation", "recommendation", "rag"]
        
        return intent if intent in valid_intents else "rag"
```

### 使用方式

```python
# backend/app/services/routing_service.py
from ..utils.intent_classifier import LLMIntentClassifier
from ..services import rag_service

class RoutingService:
    def __init__(self):
        self.intent_classifier = LLMIntentClassifier()
    
    def route_query(self, query: str, session_id: str, **kwargs):
        intent = self.intent_classifier.classify(query)
        
        if intent == "chat":
            # 纯聊天，不检索知识库
            messages = [{"role": "user", "content": query}]
            answer, _ = chat(messages, **kwargs)
            return {"answer": answer, "sources": [], "intent": intent}
            
        elif intent == "weather":
            # 调用天气API
            weather_data = self.get_weather(query)
            return {"answer": weather_data, "sources": [], "intent": intent}
            
        else:
            # 走RAG流程
            generator = rag_service.ask_rag_stream(query, session_id, **kwargs)
            return generator

# 在chat_router.py中使用
from ..services.routing_service import RoutingService

router_service = RoutingService()

@router.post("/chat/stream")
def chat_stream(req: ChatRequest):
    generator = router_service.route_query(
        question=req.question,
        session_id=req.session_id,
        top_k=req.top_k,
        enable_deep_think=req.enable_deep_think,
        model=req.model
    )
    return StreamingResponse(...)
```

### 优点
- 理解语义，准确率高
- 易于扩展新的意图
- 不需要手动维护规则

### 缺点
- 需要额外调用LLM，增加延迟
- 成本较高（需要支付API费用）
- 可能需要调优提示词

---

## 方案三：混合方法（最佳实践）

### 适用场景
- 平衡性能和准确性
- 有明确的快速判断规则
- 复杂情况回退到LLM

### 实现方式

```python
# backend/app/utils/intent_classifier.py
class HybridIntentClassifier:
    def __init__(self, llm_model="glm-4.5-flash"):
        # 第一层：快速规则判断
        self.rule_classifier = RuleBasedIntentClassifier()
        
        # 第二层：LLM分类器（仅在规则不确定时使用）
        self.llm_classifier = LLMIntentClassifier(llm_model)
        
        # 设置规则置信度阈值
        self.confidence_threshold = 2
        
    def classify(self, query: str) -> dict:
        # 1. 先用规则分类
        rule_intent, rule_score = self.rule_classifier.classify_with_score(query)
        
        # 2. 如果规则置信度高，直接返回
        if rule_score >= self.confidence_threshold:
            return {
                "intent": rule_intent,
                "confidence": rule_score,
                "method": "rule"
            }
        
        # 3. 规则不确定时，用LLM确认
        llm_intent = self.llm_classifier.classify(query)
        
        return {
            "intent": llm_intent,
            "confidence": 0.8,  # LLM返回的置信度
            "method": "llm"
        }
```

### 使用方式

```python
# 在routing_service.py中使用
from ..utils.intent_classifier import HybridIntentClassifier

class RoutingService:
    def __init__(self):
        self.intent_classifier = HybridIntentClassifier()
    
    def route_query(self, query: str, session_id: str, **kwargs):
        result = self.intent_classifier.classify(query)
        intent = result["intent"]
        method = result["method"]
        
        # 记录分类方法和置信度
        logger.info(f"Query: {query}, Intent: {intent}, Method: {method}")
        
        if intent == "chat":
            # 处理纯聊天
            return self.handle_chat(query)
        elif intent == "weather":
            # 处理天气查询
            return self.handle_weather(query)
        else:
            # RAG流程
            return self.handle_rag(query, session_id, **kwargs)
```

### 优点
- 快速查询使用规则，延迟低
- 复杂查询使用LLM，准确率高
- 成本可控（大部分查询不调用LLM）
- 可监控和优化

### 缺点
- 实现稍复杂
- 需要维护两套逻辑

---

## 方案四：向量数据库意图分类（高级）

### 适用场景
- 有大量历史查询数据
- 意图类型较多且边界模糊
- 希望自动发现新的意图模式

### 实现方式

```python
# backend/app/utils/vector_intent_classifier.py
import numpy as np
from ..store.vector_store import get_vector_store

class VectorIntentClassifier:
    def __init__(self):
        self.vector_store = get_vector_store()
        self.intent_examples = self._load_intent_examples()
    
    def _load_intent_examples(self):
        # 预先定义每个意图的代表示例
        examples = {
            "chat": ["你好", "最近怎么样", "聊聊天气"],
            "document_search": ["帮我找项目文档", "查找会议记录", "文档在哪"],
            "weather": ["今天天气如何", "明天会下雨吗", "气温多少度"],
            "calculation": ["1+1等于多少", "计算23*45", "算一下利息"],
            "recommendation": ["推荐一本书", "有什么好电影", "给我建议"]
        }
        
        # 为每个意图创建向量表示
        intent_vectors = {}
        for intent, texts in examples.items():
            vectors = [self.vector_store.embed(text) for text in texts]
            # 使用平均向量作为意图原型
            intent_vectors[intent] = np.mean(vectors, axis=0)
        
        return intent_vectors
    
    def classify(self, query: str):
        # 1. 查询向量化
        query_vector = self.vector_store.embed(query)
        
        # 2. 计算与各意图的相似度
        similarities = {}
        for intent, intent_vector in self.intent_examples.items():
            similarity = self._cosine_similarity(query_vector, intent_vector)
            similarities[intent] = similarity
        
        # 3. 返回最相似的意图
        best_intent = max(similarities, key=similarities.get)
        confidence = similarities[best_intent]
        
        return {
            "intent": best_intent,
            "confidence": confidence,
            "all_scores": similarities
        }
    
    def _cosine_similarity(self, vec1, vec2):
        return np.dot(vec1, vec2) / (np.linalg.norm(vec1) * np.linalg.norm(vec2))
```

### 优点
- 基于语义相似度，准确率高
- 可以自动扩展新的意图
- 不需要维护规则或提示词
- 可以持续学习改进

### 缺点
- 需要预先定义意图示例
- 依赖向量嵌入质量
- 对新意图类型需要手动添加示例

---

## 方案选择建议

### 选择方案一（规则方法）如果：
- ✅ 预算有限
- ✅ 意图类型少且明确
- ✅ 需要快速上线
- ✅ 对延迟敏感

### 选择方案二（LLM方法）如果：
- ✅ 意图类型较多
- ✅ 有足够的预算
- ✅ 需要高准确率
- ✅ 系统需要良好的扩展性

### 选择方案三（混合方法）如果：
- ✅ 希望平衡性能和准确率
- ✅ 大部分查询简单，少数复杂
- ✅ 需要成本控制
- ✅ 希望系统有可观测性

### 选择方案四（向量方法）如果：
- ✅ 有大量历史数据
- ✅ 意图边界模糊
- ✅ 希望自动发现新意图
- ✅ 团队有ML经验

---

## 实施步骤建议

### 阶段一：需求分析（1-2天）
1. 确定需要识别的意图类型
2. 分析历史查询数据
3. 定义每个意图的处理逻辑

### 阶段二：选择方案（1天）
- 根据上述建议选择合适的方案
- 评估资源需求（人力、预算、时间）

### 阶段三：实现核心逻辑（2-3天）
1. 实现意图分类器
2. 实现路由逻辑
3. 集成到现有系统

### 阶段四：测试优化（1-2天）
1. 准备测试用例
2. 性能测试
3. 准确率优化

### 阶段五：监控维护（持续）
1. 添加日志记录
2. 收集反馈数据
3. 持续优化分类器

---

## 注意事项

1. **性能影响**：意图识别会增加系统延迟，需要权衡
2. **成本控制**：LLM方法会增加API调用成本
3. **错误处理**：要有默认处理逻辑，避免分类错误导致系统异常
4. **用户体验**：意图分类对用户应该是透明的
5. **可解释性**：建议记录分类结果，便于调试和优化

建议从方案一或方案三开始实施，根据实际效果再决定是否升级到更复杂的方案。