# ============================================
# my-rag 依赖清单
# 安装：pip install -r requirements.txt
# Python 版本：3.12+
# ============================================

# ============================================
# 基础依赖
# ============================================
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ============================================
# LangChain 生态（核心 RAG 框架）
# ============================================
langchain>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0
langchain-ollama>=0.2.0
langchain-chroma>=0.1.0

# ============================================
# 文档加载 / 解析
# ============================================
pypdf>=5.0.0
python-docx>=1.1.0
docx2txt>=0.8                  # LangChain Docx2txtLoader 依赖
openpyxl>=3.1.0
beautifulsoup4>=4.12
lxml>=5.0
markdown>=3.7
# unstructured>=0.15.0          # 已移除：Windows 下依赖过于复杂，改用轻量方案

# ============================================
# 向量数据库（ChromaDB - 零配置本地文件存储）
# ============================================
chromadb>=0.5.0

# ============================================
# 数据处理工具
# ============================================
python-multipart>=0.0.9
python-dotenv>=1.0.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
httpx>=0.27.0
tiktoken>=0.7.0
jieba>=0.42.1

# ============================================
# 调试 / 观测
# ============================================
# LangSmith 调用链追踪（用户已有 Key，直接开启）
langsmith>=0.1.0

# ============================================
# 可选：联网搜索（加分项 E04 查询扩展 / 联网 RAG）
# ============================================
# tavily-python>=0.5.0

# ============================================
# 可选：本地嵌入 BGE-M3（纯离线，需下载模型）
# 如果使用 SiliconFlow API 则不需要下面这些
# ============================================
# sentence-transformers>=3.1.0
# FlagEmbedding>=1.2.11
# transformers>=4.45
# torch>=2.4

# ============================================
# 可选：重排序模型（提升检索精度）
# ============================================
# sentence-transformers>=3.1.0
# FlagEmbedding>=1.2.11
# rank-bm25>=0.2.2

# ============================================
# 可选：其他向量数据库
# ============================================
# qdrant-client>=1.11.0
# faiss-cpu>=1.8.0
# pymilvus>=2.4.0
